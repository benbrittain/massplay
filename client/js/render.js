/*
 * Canvas functions to handle the rendering of bitmaps.
 */

/* Initialize canvas objects */
window.onload = function () {
  var c = document.getElementById('game');
  var ctx = c.getContext('2d');
  ctx.fillStyle = '#f00";
  ctx.fillRect(5,5,95,95);
  
}
